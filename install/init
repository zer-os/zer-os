if ! command -v paru >/dev/null 2>&1; then
echo "installing paru"
zerossudo pacman -S --needed base-devel
echo ""
git clone https://aur.archlinux.org/paru.git ~/.zerosparu
cd ~/.zerosparu
PACMAN_AUTH="zerossudo" makepkg -si --noconfirm
cd ~
rm -rf ~/.zerosparu
echo ""
fi
