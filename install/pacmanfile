if ! command -v pacmanfile >/dev/null 2>&1; then
echo "installing pacmanfile"
curl -s https://raw.githubusercontent.com/cloudlena/pacmanfile/refs/heads/main/pacmanfile -o /tmp/pacmanfile
chmod +x /tmp/pacmanfile
zerossudo mv /tmp/pacmanfile /usr/local/bin/pacmanfile
fi
